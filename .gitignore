node_modules/
dist/
build/
.next/
coverage/
*.log

# Secrets. Every .env flavour stays local; only the example is versioned.
.env
.env.local
.env.*.local
.env.*
!.env.example

# Local-only: operational scripts are run by hand rather than shipped.
scripts/

# Local/agent documentation - not for remote
docs/

# Local dev scratch: records whichever port the frontend last used. Nothing
# reads it, and a committed copy just tracks one machine's state.
.fe-port

# graphify
graphify-out/
.graphify/

# Test files (local only)
**/__tests__/**
**/*.test.js
**/*.test.jsx
**/*.test.ts
**/*.test.tsx
**/*.spec.js
**/*.spec.jsx
**/*.spec.ts
**/*.spec.tsx
