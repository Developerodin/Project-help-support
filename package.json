{
  "name": "project-management-system",
  "private": true,
  "version": "1.0.0",
  "workspaces": ["shared", "backend", "frontend"],
  "engines": { "node": ">=20.0.0" },
  "scripts": {
    "dev": "npm run dev -w @pms/frontend",
    "dev:backend": "npm run dev -w @pms/backend",
    "test": "npm test --workspaces --if-present",
    "lint": "eslint ."
  },
  "devDependencies": {
    "eslint": "^9.17.0",
    "globals": "^14.0.0"
  }
}
